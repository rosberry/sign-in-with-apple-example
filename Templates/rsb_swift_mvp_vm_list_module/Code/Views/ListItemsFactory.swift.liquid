{% include 'header' %}

import CollectionViewTools

final class {{ prefix }}{{ module_info.name }}ListItemsFactory {

	weak var viewController: {{ prefix }}{{ module_info.name }}ViewController?
	weak var output: {{ prefix }}{{ module_info.name }}ViewOutput?

	func makeSectionItems(state: {{ prefix }}{{ module_info.name }}State) -> [GeneralCollectionViewDiffableSectionItem] {
		return []
	}
}
